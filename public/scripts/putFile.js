'use strict';

document.addEventListener("DOMContentLoaded", event => {


  document.getElementsByClassName('save-file-btn').addEventListener('click', event => {
    event.preventDefault();
    console.log('==================================================');
    // userSession.putFile(path: string, content: string);
  });
});
